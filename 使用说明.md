# Survey Pipeline — 使用说明

## 项目简介

自动化综述生成系统，持续跟踪 **Code Generation** 方向的 arXiv 论文，通过 DeepSeek API 进行结构化分析、聚类分类，生成每周更新的综述摘要。

---

## 快速开始

### 1. 安装依赖
```bash
cd survey_pipeline
pip install -r requirements.txt
```

### 2. 配置 API Key
创建 `.env` 文件，填入 DeepSeek API Key：
```
DEEPSEEK_API_KEY=sk-你的key
```
获取地址：https://platform.deepseek.com

### 3. 运行
```bash
# 一键运行全流程
python -m src.pipeline --full

# 单独运行某个模块
python -m src.pipeline --step fetcher    # 抓取论文
python -m src.pipeline --step cards      # 生成论文卡片
python -m src.pipeline --step cluster    # 聚类分类
python -m src.pipeline --step compare    # 方法对比表
python -m src.pipeline --step digest     # 生成周综述
python -m src.pipeline --step report     # 生成最终报告

# 增量更新（只抓新论文）
python -m src.pipeline --incremental
```

### 4. 启动前端查看
```bash
python -m src.viewer
```
浏览器打开 http://127.0.0.1:8080

---

## 前端页面说明

| 标签页 | 内容 |
|--------|------|
| **Dashboard** | 统计概览（论文数、分类分布），点击分类可跳转到对应论文 |
| **Paper Cards** | 每篇论文的 11 字段结构化卡片，支持搜索过滤，点击标题跳转 arXiv 原文 |
| **Comparison** | 方法对比表（复杂度、场景、优缺点） |
| **Weekly Digest** | 每周综述，顶部按钮可切换不同周（W21 → W22 → W23） |
| **Final Report** | 整合所有周综述的最终 6-10 页报告 |

---

## 配置说明

编辑 `config.yaml`：

| 配置项 | 说明 |
|--------|------|
| `arxiv.search_query` | arXiv 搜索关键词（换研究方向时修改这里） |
| `arxiv.max_results` | 每次抓取论文数量 |
| `api.deepseek.model_cards` | 论文分析模型 |
| `clustering.min_cluster_size` | 最小聚类大小 |

---

## 自动更新

- **GitHub Actions** 每周一北京时间 8:00 自动运行
- 抓取最新论文 → 生成卡片 → 聚类分析 → 生成综述 → 提交回仓库
- 手动触发：GitHub → Actions → Weekly Survey Pipeline → Run workflow

---

## 输出文件

| 文件 | 说明 |
|------|------|
| `data/papers_raw.json` | 原始论文数据（标题、作者、摘要） |
| `data/paper_cards.jsonl` | 结构化论文卡片（11 字段） |
| `data/taxonomy.md` | 研究分类体系 |
| `data/comparison_table.csv` | 方法对比表 |
| `data/weekly/digest_*.md` | 每周综述 |
| `output/final_report.md` | 最终综述报告 |

---

## 环境要求

- Python ≥ 3.12
- DeepSeek API Key
- 网络可访问 arXiv API 和 DeepSeek API
